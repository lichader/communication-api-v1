pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Start building"
            }
        }
        stage('Publish') {
            steps {
                echo "Start publishing"
            }
        }
    }
}